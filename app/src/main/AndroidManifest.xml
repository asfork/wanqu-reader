<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.steve.wanqureader">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WanquApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="DATABASE"
            android:value="wanqu.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.steve.wanqureader.storage.model" />

        <activity
            android:name=".presentation.ui.activities.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.activities.WebViewActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".presentation.ui.activities.FrontIssuesActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".presentation.ui.activities.StarredActivity"
            android:launchMode="singleTask" />
    </application>
</manifest>